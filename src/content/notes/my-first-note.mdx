---
title: "My First Note"
subtitle: "A small example note to use as a starting point"
date: "2026-06-03"
published: true
---

This is an example note. You can write normal Markdown here, and the page will render it as MDX.

## A section heading

Use paragraphs, lists, links, blockquotes, and code blocks as usual.

- First idea
- Second idea
- Third idea

> This is a blockquote for calling out an idea.

Inline code like `const message = "hello"` works too.

```tsx
export function Example() {
  return <p>Hello from MDX.</p>;
}
```

Because `published` is set to `true`, this note appears on `/notes`.
